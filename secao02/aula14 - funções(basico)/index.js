// function soma(a,b){
//     return a + b; //tudo o que está abaixo de return não é executado
// }

// console.log(soma(2,2));

// Função Anônima -> Jogar uma função dentro de uma variável
const raiz = (n) => {
    return Math.sqrt(n);
};

console.log(raiz(9));